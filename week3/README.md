# workshop 1
- สร้าง for loop วนเลขตั้งแต่ 1-50
- สร้างเงื่อนไขแบ่งเป็น 2 เงื่อนไข
    1. ถ้าเป็นเลขคู่ ตั้งแต่ 2-50 ให้หาผลรวมของเลขคู่ทั้งหมด แล้วเก็บผลลัพธ์ไว้ที่ `sumEven`
    2. ถ้าเป็นเลขคี่ ที่น้อยกว่าหรือเท่ากับเลข 10 ให้เอาเลขมาคูณกันแล้วเก็บผลลัพธ์ไว้ที่ `productOdd`

# workshop 2
- สร้างฟังก์ชันคำนวณค่า รับค่า parameter เป็น weight และ height
- คำนวณ BMI และสร้างเงื่อนไขเช็ค bmi ว่า category อยู่ในเกณฑ์ไหน
- returm ค่า bmi และ category เป็น object